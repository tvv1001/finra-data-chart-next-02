# Legacy scripts archive

One-off crawl/fix/patch/theme/push/deploy scripts and retired tooling. Includes the bulk of former `scripts/` after the nuclear day-to-day trim (dev/build/test only in active `scripts/` + `package.json`).

Hardcoded per-CRD sample JSON dumps under old `data-dumps/` were **deleted**, not kept.

These are **not** wired into `package.json`. Active keep-list: `scripts/README.md`.

Safe to delete this folder later once you confirm you do not need any of these by hand.
