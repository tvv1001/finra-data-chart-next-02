// For main selection of the centered node (how many hops to highlight when a
// node is selected in the center), use DEFAULT_SELECTION_HOPS.
export const DEFAULT_SELECTION_HOPS = 2;

// For expansion operations (nodes that have a parent or when expanding a
// selected node's neighborhood), use DEFAULT_EXPANSION_HOPS. This is the
// default number of hops requested from the server during expansion flows.
export const DEFAULT_EXPANSION_HOPS = 1;
